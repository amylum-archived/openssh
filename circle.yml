machine:
  services:
  - docker
  environment:
    GIT_COMMITTER_NAME: 'Les Aker'
    GIT_COMMITTER_EMAIL: 'me@lesaker.org'
dependencies:
  override:
  - git submodule update --init
test:
  override:
  - make
