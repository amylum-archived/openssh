machine:
  services:
  - docker
dependencies:
  override:
  - git submodule update --init
  - git fetch --tags origin
test:
  override:
  - make
deployment:
  all:
    branch: /.*/
    commands:
    - cp -r pkg/* $CIRCLE_ARTIFACTS/
  release:
    tag: /.*/
    commands:
    - make release
